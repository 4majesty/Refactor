package org.eclipse.jdt.internal.corext.refactoring.code.flow;

import org.eclipse.jdt.core.dom.IMethodBinding;
import org.eclipse.jdt.core.dom.ITypeBinding;

public interface IMessageSendFlowInfo {
	void mergeArgument(FlowInfo info, FlowContext context);

	void mergeReceiver(FlowInfo info, FlowContext context);

	void mergeExceptions(IMethodBinding binding, FlowContext context);
}
